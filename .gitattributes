# Тримаємо LF для всіх текстових файлів (запускаються в Linux-контейнері)
* text=auto eol=lf
*.png binary
# Вендорені DejaVu: git не має права їх чіпати — їх SHA-256 перевіряється на старті
*.ttf binary
